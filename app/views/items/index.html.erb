<%= render "shared/header" %>

<%= link_to '追加', new_item_path %>

<% if @items != nil %>
  <div>
    <table>
      <tr>
        <th>品名</th><th>量</th><th>店舗名（指定ある場合）</th>
      </tr>
  <% @items.each do |item| %>
      <tr>
        <td><%= item.user.nickname %></td><td><%= item.item_name %></td><td><%= item.volume %></td><td><%= item.item_s_name %><%= item.item_s_branch %></td><br>
      </tr>
    </table>
  </div>
  <% end %>
<% end %>